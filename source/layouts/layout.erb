<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="shortcut icon" href="assets/favicon.ico">

    <!-- Always force latest IE rendering engine or request Chrome Frame -->
    <meta content="IE=edge,chrome=1" http-equiv="X-UA-Compatible">

    <!-- Use title if it's in the page YAML frontmatter -->
    <title><%= current_page.data.title || "Being Brave" %></title>

    <!-- CSS -->
    <%= stylesheet_link_tag "bootstrap.min", "font-awesome.min", "normalize", "all" %>
    <%= stylesheet_link_tag "https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css" %>
    <%= stylesheet_link_tag "https://fonts.googleapis.com/css?family=Montserrat:400,700" %>

    <script>
      // window.addEventListener("hashchange", function() { scrollBy(0, -30) });

      // var shiftWindow = function() { scrollBy(0, -50) };
      // if (location.hash) shiftWindow();
      // window.addEventListener("hashchange", shiftWindow);
    </script>

  </head>

  <body class="<%= page_classes %>" data-spy="scroll" data-target="#navbar-scroll">
    <%= partial "layouts/_navbar" %>
    <%= yield %>
    <%= partial "layouts/_footer" %>

    <!-- jQuery / JavaScript -->
    <!-- <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script> -->
    <%= javascript_include_tag "jquery-1.11.3.min", "bootstrap.min" %>

    <script>
    function scrollWin(x, y) {
        window.scrollBy(x, y);
    }
    </script>

    </body>
</html>
